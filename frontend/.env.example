# Vite development server URL used by the frontend.
# The frontend only talks to the FastAPI backend over HTTP.
# Never put LLM API keys in this file or anywhere else in the frontend.
VITE_API_BASE_URL=http://localhost:8000
